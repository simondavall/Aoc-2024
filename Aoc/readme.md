This project builds all Days and Executes them with execution timings.
It is called from the root makefile.
To run without using the makefile, open the makefile and copy the command line dotnet instruction, and run it directly in the command line.
